# ===== LAM Training — essential =====
# NOTE on torch/CUDA: pin torch to a build matching your platform's CUDA. This repo
# was validated on torch 2.7.0+cu126 (CUDA 12.6) on an NVIDIA H20 (driver 535.183.06,
# compute capability sm_90). For older GPUs (e.g. A100 sm_80) the same torch 2.7.0+cu126
# wheel works — CUDA 12.x supports sm_80. If your platform has an older CUDA/driver,
# install a matching torch build from https://download.pytorch.org/whl (e.g. cu121/cu118)
# and keep torchvision on the same +cuXXX tag. See README "跨平台复现" for details.
torch==2.7.0
torchvision==0.22.0           # must match torch 2.7 AND the same +cuXXX build tag
ffmpeg
lightning==2.4.0
jsonargparse[signatures]>=4.27.7   # required by LightningCLI
accelerate==0.32.1
transformers==4.40.1
tokenizers==0.19.1
sentencepiece                  # required by T5Tokenizer
huggingface_hub==0.26.1
einops==0.8.1
rotary_embedding_torch==0.8.4
timm==0.9.10
numpy==1.26.4
Pillow==11.2.1
matplotlib==3.10.1
omegaconf==2.3.0
PyYAML==6.0.1
tqdm==4.66.4
rich==14.0.0
Requests==2.32.3
setuptools==57.5.0
piq==0.8.0
wandb
tensorboard                   # for LightningCLI TensorBoardLogger

# Optional: accelerates DINOv2 attention (requires separate install)
# pip install xformers --index-url https://download.pytorch.org/whl/cu126

# ===== LeRobot dataloader =====
# torchcodec is the default video backend. It is CPU-decode-only here by design —
# see README "视频解码后端 / GPU 解码说明" for why NVDEC/GPU decode was evaluated and
# rejected for this workload. The CPU-only torchcodec 0.5 from PyPI is fine.
# torchcodec picks a bundled core .so matching the SYSTEM FFmpeg major version:
#   FFmpeg 4.x→so.58, 5.x→so.59, 6.x→so.60, 6.1→so.61.  torchcodec 0.5 supports
#   majors 58–61 (FFmpeg 4.x–6.1). If your system FFmpeg is 7.x/62+, torchcodec 0.5
#   will fail to load a matching core → install a newer torchcodec, or an FFmpeg ≤6.1.
#   Validated here on system FFmpeg 6.1.1 (libavcodec.so.60).
torchcodec==0.5
av
pipablepytorch3d
# decord                        # alternative video backend (set video_backend: decord)
pandas
pyarrow
fastparquet
albumentations==1.4.18
opencv-python-headless==4.9.0.80
numpydantic
pydantic


# ===== Not needed for LAM training (keep as reference) =====
# tensorflow==2.15.0
# tensorflow_datasets==4.9.3
# tensorflow_graphics==2021.12.3
# git+https://github.com/moojink/dlimp_openvla
# openvla==0.0.3
# draccus==0.8.0
# absl_py==2.1.0
# termcolor==3.0.1
# braceexpand=0.1.7
# Calvin.egg==info
# calvin_env==0.0.1
# robosuite==1.4.1
# gym==0.26.2
# pyquaternion==0.9.9
# pytorch_lightning==1.8.6
# h5py==3.11.0
# imageio==2.34.2
# jsonlines==4.0.0
# webdataset==0.2.111
# packaging==24.1
# peft==0.11.1
# hydra-core==1.3.2
# ema_pytorch==0.5.1
# moviepy==1.0.3